from django.contrib import admin
from django.contrib.auth.admin import UserAdmin
from django.contrib.auth.models import User

# The default UserAdmin is already registered, so we don't need to do anything extra.
# This file is here for future customization.
